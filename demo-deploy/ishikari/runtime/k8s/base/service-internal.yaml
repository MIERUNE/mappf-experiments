apiVersion: v1
kind: Service
metadata:
  name: ishikari-internal
  labels:
    app: ishikari
spec:
  type: ClusterIP
  selector:
    app: ishikari
  ports:
    - name: internal
      port: 9090
      targetPort: internal
