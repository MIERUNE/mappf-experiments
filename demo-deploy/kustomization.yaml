apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Combined GKE demo stack. Service-owned overlays remain independently
# deployable; this composes them with the shared namespace and Gateway.
resources:
  - platform
  - ishikari/runtime/k8s/overlays/gke
  - biei/runtime/k8s/overlays/gke
