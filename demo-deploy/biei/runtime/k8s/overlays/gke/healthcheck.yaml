apiVersion: networking.gke.io/v1
kind: HealthCheckPolicy
metadata:
  name: biei
spec:
  default:
    config:
      type: HTTP
      httpHealthCheck:
        requestPath: /readyz
        port: 8080
  targetRef:
    group: ""
    kind: Service
    name: biei
